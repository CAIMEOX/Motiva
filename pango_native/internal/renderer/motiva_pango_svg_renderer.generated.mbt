// GENERATED by gir/subclass — DO NOT EDIT

///|
/// Concrete GObject subclass whose immediate parent is @pango.Renderer.
pub type MotivaPangoSvgRenderer

///|
/// Common object view for APIs that accept this generated subclass.
pub(open) trait IMotivaPangoSvgRenderer: @pango.IRenderer + @gobject.IObject {
  fn as_motiva_pango_svg_renderer(Self) -> MotivaPangoSvgRenderer
}

///|
pub impl IMotivaPangoSvgRenderer for MotivaPangoSvgRenderer with fn as_motiva_pango_svg_renderer(
  self,
) {
  self
}

///|
/// The native instance is also a @pango.Renderer; every GObject instance struct begins with its parent instance.
pub fn MotivaPangoSvgRenderer::as_pango_renderer(
  self : MotivaPangoSvgRenderer,
) -> @pango.Renderer = "%identity"

///|
pub impl @pango.IRenderer for MotivaPangoSvgRenderer with fn as_renderer(self) {
  self.as_pango_renderer()
}

///|
/// The native instance is also a @gobject.Object; every GObject instance struct begins with its parent instance.
pub fn MotivaPangoSvgRenderer::as_gobject_object(
  self : MotivaPangoSvgRenderer,
) -> @gobject.Object = "%identity"

///|
pub impl @gobject.IObject for MotivaPangoSvgRenderer with fn as_object(self) {
  self.as_gobject_object()
}

///|
/// Call the inherited Renderer.prepare_run implementation saved before this subclass installed its override.
#borrow(object, run)
extern "c" fn moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_prepare_run(
  object : @pango.Renderer,
  run : @pango.GlyphItem,
) -> Unit = "moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_prepare_run"

///|
#warnings("-unused_value")
fn MotivaPangoSvgRenderer::parent_prepare_run(
  object : @pango.Renderer,
  run : @pango.LayoutRun,
) -> Unit {
  moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_prepare_run(
    object, run,
  )
}

///|
/// Call the inherited Renderer.draw_glyph_item implementation saved before this subclass installed its override.
#borrow(object, text_bytes, glyph_item)
extern "c" fn moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_glyph_item(
  object : @pango.Renderer,
  text_bytes : Bytes,
  text_has : Int,
  glyph_item : @pango.GlyphItem,
  x : Int,
  y : Int,
) -> Unit = "moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_glyph_item"

///|
#warnings("-unused_value")
fn MotivaPangoSvgRenderer::parent_draw_glyph_item(
  object : @pango.Renderer,
  text : String?,
  glyph_item : @pango.GlyphItem,
  x : Int,
  y : Int,
) -> Unit {
  let (text_bytes, text_has) = match text {
    Some(value) => (@encoding/utf8.encode(value), 1)
    None => (b"", 0)
  }
  moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_glyph_item(
    object, text_bytes, text_has, glyph_item, x, y,
  )
}

///|
/// Call the inherited Renderer.draw_rectangle implementation saved before this subclass installed its override.
#borrow(object)
extern "c" fn moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_rectangle(
  object : @pango.Renderer,
  part : @pango.RenderPart,
  x : Int,
  y : Int,
  width : Int,
  height : Int,
) -> Unit = "moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_rectangle"

///|
#warnings("-unused_value")
fn MotivaPangoSvgRenderer::parent_draw_rectangle(
  object : @pango.Renderer,
  part : @pango.RenderPart,
  x : Int,
  y : Int,
  width : Int,
  height : Int,
) -> Unit {
  moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_rectangle(
    object, part, x, y, width, height,
  )
}

///|
/// Call the inherited Renderer.draw_error_underline implementation saved before this subclass installed its override.
#borrow(object)
extern "c" fn moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_error_underline(
  object : @pango.Renderer,
  x : Int,
  y : Int,
  width : Int,
  height : Int,
) -> Unit = "moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_error_underline"

///|
#warnings("-unused_value")
fn MotivaPangoSvgRenderer::parent_draw_error_underline(
  object : @pango.Renderer,
  x : Int,
  y : Int,
  width : Int,
  height : Int,
) -> Unit {
  moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_error_underline(
    object, x, y, width, height,
  )
}

///|
/// Call the inherited Renderer.draw_trapezoid implementation saved before this subclass installed its override.
#borrow(object)
extern "c" fn moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_trapezoid(
  object : @pango.Renderer,
  part : @pango.RenderPart,
  y1_ : Double,
  x11 : Double,
  x21 : Double,
  y2 : Double,
  x12 : Double,
  x22 : Double,
) -> Unit = "moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_trapezoid"

///|
#warnings("-unused_value")
fn MotivaPangoSvgRenderer::parent_draw_trapezoid(
  object : @pango.Renderer,
  part : @pango.RenderPart,
  y1_ : Double,
  x11 : Double,
  x21 : Double,
  y2 : Double,
  x12 : Double,
  x22 : Double,
) -> Unit {
  moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_trapezoid(
    object, part, y1_, x11, x21, y2, x12, x22,
  )
}

///|
/// Call the inherited Renderer.draw_glyph implementation saved before this subclass installed its override.
#borrow(object, font)
extern "c" fn moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_glyph(
  object : @pango.Renderer,
  font : @pango.Font,
  glyph : UInt,
  x : Double,
  y : Double,
) -> Unit = "moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_glyph"

///|
#warnings("-unused_value")
fn MotivaPangoSvgRenderer::parent_draw_glyph(
  object : @pango.Renderer,
  font : &@pango.IFont,
  glyph : @pango.Glyph,
  x : Double,
  y : Double,
) -> Unit {
  moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_glyph(
    object,
    font.as_font(),
    glyph,
    x,
    y,
  )
}

///|
/// Call the inherited Renderer.draw_shape implementation saved before this subclass installed its override.
#borrow(object, attr)
extern "c" fn moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_shape(
  object : @pango.Renderer,
  attr : @pango.AttrShape,
  x : Int,
  y : Int,
) -> Unit = "moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_shape"

///|
#warnings("-unused_value")
fn MotivaPangoSvgRenderer::parent_draw_shape(
  object : @pango.Renderer,
  attr : @pango.AttrShape,
  x : Int,
  y : Int,
) -> Unit {
  moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_parent_renderer_draw_shape(
    object, attr, x, y,
  )
}

///|
/// C retains `state` until the native object's `dispose` method runs.
#borrow(state)
extern "c" fn moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_new(
  renderer_prepare_run : FuncRef[
    (@pango.Renderer, @pango.GlyphItem, MotivaPangoSvgRendererState) -> Unit,
  ],
  renderer_draw_glyph_item : FuncRef[
    (
      @pango.Renderer,
      Bytes,
      Int,
      @pango.GlyphItem,
      Int,
      Int,
      MotivaPangoSvgRendererState,
    ) -> Unit,
  ],
  renderer_draw_rectangle : FuncRef[
    (
      @pango.Renderer,
      @pango.RenderPart,
      Int,
      Int,
      Int,
      Int,
      MotivaPangoSvgRendererState,
    ) -> Unit,
  ],
  renderer_draw_error_underline : FuncRef[
    (@pango.Renderer, Int, Int, Int, Int, MotivaPangoSvgRendererState) -> Unit,
  ],
  renderer_draw_trapezoid : FuncRef[
    (
      @pango.Renderer,
      @pango.RenderPart,
      Double,
      Double,
      Double,
      Double,
      Double,
      Double,
      MotivaPangoSvgRendererState,
    ) -> Unit,
  ],
  renderer_draw_glyph : FuncRef[
    (
      @pango.Renderer,
      @pango.Font,
      UInt,
      Double,
      Double,
      MotivaPangoSvgRendererState,
    ) -> Unit,
  ],
  renderer_draw_shape : FuncRef[
    (@pango.Renderer, @pango.AttrShape, Int, Int, MotivaPangoSvgRendererState) -> Unit,
  ],
  state : MotivaPangoSvgRendererState,
) -> MotivaPangoSvgRenderer = "moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_new"

///|
/// Create one native MotivaPangoSvgRenderer and retain its MoonBit state.
pub fn MotivaPangoSvgRenderer::new(
  state : MotivaPangoSvgRendererState,
) -> MotivaPangoSvgRenderer {
  moonbit_motiva_pango_native_renderer_motiva_pango_svg_renderer_new(
    fn(object_, run, state_) {
      @pango.VRendererPrepareRun::prepare_run(state_, object_, run)
    },
    fn(object_, text_bytes, text_has, glyph_item, x, y, state_) {
      @pango.VRendererDrawGlyphItem::draw_glyph_item(
        state_,
        object_,
        if text_has == 0 {
          None
        } else {
          Some(@encoding/utf8.decode_lossy(text_bytes))
        },
        glyph_item,
        x,
        y,
      )
    },
    fn(object_, part, x, y, width, height, state_) {
      @pango.VRendererDrawRectangle::draw_rectangle(
        state_, object_, part, x, y, width, height,
      )
    },
    fn(object_, x, y, width, height, state_) {
      @pango.VRendererDrawErrorUnderline::draw_error_underline(
        state_, object_, x, y, width, height,
      )
    },
    fn(object_, part, y1_, x11, x21, y2, x12, x22, state_) {
      @pango.VRendererDrawTrapezoid::draw_trapezoid(
        state_, object_, part, y1_, x11, x21, y2, x12, x22,
      )
    },
    fn(object_, font, glyph, x, y, state_) {
      @pango.VRendererDrawGlyph::draw_glyph(state_, object_, font, glyph, x, y)
    },
    fn(object_, attr, x, y, state_) {
      @pango.VRendererDrawShape::draw_shape(state_, object_, attr, x, y)
    },
    state,
  )
}
